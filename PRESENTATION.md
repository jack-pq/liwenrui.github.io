# 「日常集」——集成式生活工作台 · 演示宣讲文稿

> 一句话定位：把记账、习惯、健身、日程、清单、书影六件生活小事，收拢到一个克制而温暖的页面里，数据落在飞书多维表格，跨设备实时一致。

---

## 一、开场（约 1 分钟）

各位好，今天向大家汇报的项目叫 **「日常集」**。

起因很简单：我手机里装了记账 App、习惯打卡 App、健身记录 App、日程 App、购物清单 App、书影标记 App——六件事，六个入口，六套账号，数据彼此看不见。于是我萌生一个念头：**能不能把它们收拢到一个页面里？**

不只是"聚合链接"那种壳子，而是真正能读写、能统计、能在手机和电脑上看到同一份数据的工作台。

这就是「日常集」要做的事。接下来我从四个方面展开：**它长什么样、它怎么工作、它有哪些设计考量、它如何部署**。

---

## 二、产品概览：它长什么样（约 3 分钟）

### 2.1 七大模块

整个工作台由一个首页加六个子页组成，对应生活的六个切面：

| 模块 | 页面 | 核心能力 |
|------|------|----------|
| 今日概览 | `/` | 实时时钟、九时段问候语、每日一句、六模块聚合卡片 |
| 记账理财 | `/finance` | 快速记账、收支饼图、月度柱图、日均支出、预算进度 |
| 习惯健康 | `/habit` | 三种打卡方式（勾选/计数/时长）、热力图、连续天数 streak |
| 减脂健身 | `/fitness` | 七日体重曲线、BMI、体重变化提示、周计划 |
| 日程统筹 | `/schedule` | 按日分组、完成率进度条、一键标记完成 |
| 待买清单 | `/shopping` | 勾选即购、分类标签、已购折叠 |
| 书影收藏 | `/collection` | 搜索、平均评分、封面墙/列表双视图 |

### 2.2 首页演示要点

打开首页，你会看到三样东西：

1. **左上角的问候**——根据当前时间在九个时段里切换：清晨好、早上好、中午好、午后、下午好、傍晚好、晚上好、夜深了……配一句每日箴言。晚上十点后自动换成晚安话语，比如"今天辛苦了，好好休息"。
2. **右上角的实时时钟**——秒级跳动，用 tabular-nums 等宽数字，不会因数字宽度变化而抖动。
3. **下方六张聚合卡片**——本月收支、习惯打卡、体重趋势、今日日程、待买清单、书影收藏。每张卡片都是入口，点击直达对应模块的详情页。

这里有个细节值得提一句：**卡片数据是并行拉取的**，每张卡片独立 loading / error / retry，一张失败不会拖垮整页，只会在这张卡片的位置显示"数据暂不可用"和一个重试按钮。

---

## 三、技术架构：它怎么工作（约 4 分钟）

### 3.1 技术栈

| 层 | 选型 | 理由 |
|----|------|------|
| 框架 | Next.js 14（App Router） | 服务端路由代理飞书 API，避免暴露凭证 |
| 语言 | TypeScript | 全量类型约束，tsc --noEmit 零错误 |
| 样式 | Tailwind CSS | 大地色系 + 文艺字体，原子化高效迭代 |
| 图表 | Recharts | 饼图、柱图、折线图，声明式配置 |
| 图标 | lucide-react | 轻量、风格统一 |
| 数据 | 飞书多维表格 | 免服务器、免数据库、天然支持多端协作 |

### 3.2 数据链路

这是整个项目的关键设计，请看这张数据流：

```
浏览器 ──(fetch)──▶ Next.js API 路由 ──(fetch)──▶ 飞书开放平台 ──▶ 多维表格
                          │
                          └─ 读取 .env.local 中的 APP_ID / APP_SECRET
                             在服务端换取 tenant_access_token
                             再调用飞书 CRUD 接口
```

**为什么这样设计？**

- 飞书的 `tenant_access_token` 需要 `app_secret`，绝不能出现在浏览器端。所以必须有一层服务端代理。
- Next.js 的 Route Handler 天生适合做这件事，无需另起 Express 服务。
- 浏览器只跟自己的 `/api/feishu` 通信，不直接接触飞书域名，也规避了 CORS。

### 3.3 飞书 SDK 封装

`lib/feishu.ts` 是整个数据层的核心，约 335 行，封装了五件事：

1. **Token 缓存**：tenant_access_token 有效期 2 小时，内存缓存并在到期前 60 秒提前刷新，避免每次请求都换 token。
2. **字段类型自适配**：飞书不同字段类型对值的格式要求不同——文本字段直接收字符串，链接字段要 `{link, text}` 对象，单选字段直接收字符串。我写了一个 `getTableFieldTypes` 先拉表结构，再用 `denormalizeFieldsByType` 按类型转换，**调用方无需关心飞书的字段格式细节**。
3. **CRUD**：list / create / update / delete 四个方法，分页自动处理。
4. **错误分类**：自定义 `FeishuError`，带 code 字段（`CONFIG_MISSING` / `TOKEN_FAILED` / `TABLE_MISSING` 等），前端可据此给出针对性提示。
5. **表名映射**：七张表通过环境变量 `FEISHU_TABLE_HABIT` 等注入 Table ID，代码里只用语义化 key。

### 3.4 前端数据 Hook

`lib/hooks.ts` 里的 `useAsync` 是所有页面的数据入口：

```ts
const { data, loading, error, reload } = useAsync(() => listRecords("finance"), []);
```

返回 `reload` 方法，失败时用户可一键重试。页面先渲染骨架屏（Skeleton），再填充数据，**界面永远先出现，不会白屏等待**。

---

## 四、设计考量：它有哪些细节（约 4 分钟）

### 4.1 视觉风格：克制与温度

我刻意避开了两种常见风格：一是 SaaS 后台那种冷冰冰的蓝白卡片，二是消费 App 那种高饱和度的渐变 banner。

最终选了**大地色系**，在 tailwind.config.ts 里定义了三组：

- `ink`：墨色系，从 #f7f6f3 到 #1a1916，做文字和边框
- `clay`：陶土色，做强调和支出
- `sage`：鼠尾草绿，做收入和正向反馈

整体观感是纸张感、低饱和、有呼吸。卡片用 `shadow-soft`（两层极淡阴影），hover 时升到 `shadow-lift`，不用 border，靠阴影区分层次。

### 4.2 文艺字体

- **正文**：霞鹜文楷（LXGW WenKai），一款开源的楷体风格屏幕字体，比宋体更亲切，比黑体更文气
- **标题与引言**：思源宋体（Noto Serif SC），衬线体增加正式感
- 每日一句用 `font-serif` 包裹，配书名号「」，强化"手记"感

### 4.3 无障碍（Accessibility）

这是我特意投入的一块，通过 `AccessibilityPanel` 组件提供四项设置：

1. **字体大小**：小 / 标准 / 大 / 特大（15px → 20px），改 `<html>` 的 font-size，rem 全局响应
2. **高对比度**：切换 `.high-contrast` 类，加深文字色、减淡背景
3. **减少动画**：切换 `.reduce-motion` 类，关闭 fadeIn / slideUp
4. **键盘导航**：skip link 跳过导航直达正文，所有交互元素有可见焦点环

设置存在 localStorage，刷新不丢失。面板入口固定在右下角，aria-label 完整。

### 4.4 响应式

- 首页卡片：手机 1 列、平板 2 列、桌面 3 列
- 导航：桌面横向、手机折叠
- 图表：容器宽度自适应，Recharts 的 ResponsiveContainer

### 4.5 健壮性细节

- **错误隔离**：首页六张卡片独立拉取，一张失败不影响其他
- **乐观更新**：记账、打卡等高频操作先改 UI 再等接口返回，失败回滚并 Toast 提示
- **空状态**：每个列表页都有 EmptyState 组件，带引导文案和图标，不是冷冰冰的"暂无数据"
- **字段名适配**：飞书表的实际字段名（如"是否激活"而非"启用"）在代码里已全部对齐，避免写入失败

---

## 五、部署方案：它如何上线（约 2 分钟）

项目支持两种部署方式，都已在仓库里备好文件：

### 5.1 Vercel（推荐，零配置）

```bash
npm run build
# 推送到 GitHub，Vercel 自动构建
# 在 Vercel 后台填入 9 个环境变量即可
```

### 5.2 Docker（自托管）

仓库提供 `Dockerfile`（多阶段构建）和 `docker-compose.yml`：

```bash
cp .env.docker.example .env.docker
# 编辑 .env.docker 填入飞书凭证
docker compose up -d
```

关键点：`next.config.js` 已开启 `output: "standalone"`，构建产物自包含 Node 运行时，镜像体积可压到 200MB 以内。docker-compose 还配了 healthcheck，容器异常会自动重启。

完整步骤见 `DEPLOY.md`。

---

## 六、技术亮点回顾（约 1 分钟）

最后用五点收尾，这是我认为这个项目最值得讲的地方：

1. **飞书字段类型自适配**——`getTableFieldTypes` + `denormalizeFieldsByType`，让业务代码完全不用关心飞书的字段格式，写入时按表结构自动转换。这是踩了若干坑之后沉淀下来的，habit_log 表初始只有 1 个字段，还是通过 API 自动补建了 4 个字段。

2. **服务端凭证隔离**——app_secret 永远不出服务端，浏览器只跟 `/api/feishu` 通信，安全且无 CORS 困扰。

3. **错误隔离与重试**——首页六卡片独立 loading/error/retry，单点失败不扩散；所有数据页都有 RetryError 组件。

4. **无障碍四件套**——字体大小、高对比度、减少动画、键盘导航，不是摆设，是真正可用的辅助功能。

5. **standalone Docker 部署**——一个 `docker compose up` 就能跑起来，镜像自包含，带 healthcheck，适合内网或私有云场景。

---

## 七、结语（约 30 秒）

「日常集」这个名字，取自"日常的集合"。它不追求替代任何一个专业 App，只想做一件事：**让你在一个页面里，看见自己生活的全貌**。

记账看见花销，习惯看见坚持，体重看见自律，日程看见计划，清单看见需求，书影看见趣味——六张卡片，六个切面，拼成一个普通人的一天。

谢谢各位。

---

## 附：演示动线建议

若现场演示，建议按以下顺序操作，约 5 分钟可走完：

1. 打开首页 → 指出问候语随时段变化、时钟跳动、每日一句
2. 点击"本月收支"卡片 → 进入记账页 → 用快速记账按钮记一笔 → 看饼图实时更新
3. 进入习惯页 → 打一次卡 → 指出连续天数 streak 变化
4. 进入健身页 → 指出体重曲线和"较上周变化 X kg"提示
5. 进入日程页 → 勾选完成一个事项 → 看完成率进度条上涨
6. 进入书影页 → 用搜索框过滤 → 切换封面墙/列表视图
7. 回到首页 → 指出刚才的操作已反映在聚合卡片上（数据联动）
8. 点击右下角无障碍图标 → 切换字体大小/高对比度 → 展示可访问性
